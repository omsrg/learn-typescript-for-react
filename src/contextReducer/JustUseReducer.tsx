import { useReducer } from 'react';

// const initialState = {
// 	cookies: 1
// }

// type ActionType = {type: 'grab', payload: number} | {type: 'eat', payload: number}

// function cookiesReducer (state: typeof initialState, action: ActionType) {
// 	switch(action.type) {
// 		case 'grab':
// 			return {
// 				...state,
// 				cookies: state.cookies + action.payload
// 			};
// 		case 'eat':
// 			return {
// 				...state,
// 				cookies: state.cookies - action.payload
// 			}
// 		default:
// 			throw new Error('no action')
// 	}
// }

// const UseReducerExample = () => {
// 	const [states, Dispatch] = useReducer(cookiesReducer, initialState)
// }

// export default UseReducerExample

const Whatever = () => {
	return <h1></h1>;
};
